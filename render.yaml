services:
  - type: web
    name: yeshiva-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: yeshiva-db
          property: connectionString

databases:
  - name: yeshiva-db
    plan: free
